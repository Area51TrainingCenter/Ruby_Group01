<%= simple_form_for(@photo) do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.input :title %>
  </div>
  <div class="field">
    <%= f.input :copyright %>
  </div>
  <div class="field">
    <%= f.input :taken_at %>
    <%= f.input :iso, as: :numeric %>
    <%= f.input :aperture %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
